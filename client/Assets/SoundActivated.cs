using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;
using UnityEngine.UI;

public class SoundActivated : MonoBehaviour
{
    public Toggle musicToggle;

    /*public void SetMusic()
    {
        if (musicToggle.isOn)
        {
            Music.MusicInstance.audioSource.Play();
        }
        else
        {
            Music.MusicInstance.audioSource.Pause();
        }
    }
    */
}
